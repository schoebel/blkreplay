replay_host_list="test-sata"
replay_device_list="/dev/dm-2"

# Halve the replay time on sata, when running burst loads.
# It makes no sense to run it longer, because overload is overload
# and remains overload.

[[ "$input_file_list" =~ "bursts" ]] && (( replay_duration /= 2 ))
